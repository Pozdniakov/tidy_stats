# Отчёт вычитки: 040-if_for.qmd
Дата: 2026-05-31

## Механические проверки (bash)

- **Двойное тире `--` вместо em dash `---`:** строки 153, 155, 157, 227, 242 (5 вхождений)
- **Дефис в числовых диапазонах:** только в URL (2021-02) — ложное срабатывание
- **ifelse():** все вхождения намеренны (глава про ifelse)
- **Cross-references:** все 8 использованных ссылок ведут на другие главы (@sec-apply_f, @sec-beyond_base_r, @sec-coercion, @sec-create_fun, @sec-functional, @sec-na, @sec-pipe, @sec-tidy_join) — проверить при полной сборке
- **Мёртвые якоря:** sec-for, sec-ifelse, sec-loops_conditions, sec-sec-if — используются из других глав, не проблема
- **Слова-паразиты, Вы/Вам, p-value, Unicode-греческие, fig.cap=, устаревший синтаксис чанков:** чисто

## 🔴 Критичные

1. **[строка 184]** «(о них пойдет речь далее --- см. @sec-beyond_base_r, то я советую» → незакрытая скобка, предложение грамматически сломано. Исправить: «...@sec-beyond_base_r), то я советую». **(3/3)**

2. **[строка 18]** «если строчка всего в выражении всего одна» → двойное «всего», опечатка. Исправить: «если в выражении всего одна строчка». **(3/3)**

3. **[строка 50]** «Вот как выглядит ее применение» → «ее» относится к «оператор» (м.р.). Исправить: «его применение». **(1/3, перепроверено)**

4. **[строка 225]** `## Осторожно: не забывайте про запятые` → заголовок callout-important без курсива. Исправить: `## *Осторожно:* не забывайте про запятые`. **(3/3)**

5. **[строка 3]** Якорь `{#sec-sec-if}` — задвоенный префикс `sec-sec`. Должно быть `{#sec-if}` или `{#sec-if-else}`. **(2/3)**

## 🟡 Важные

1. **[строки 153, 155, 157, 227, 242]** `--` вместо `---` (em dash). Всего 5 вхождений. Заменить на `---`. **(2/3 + bash)**

2. **[строка 113]** «маленький - для следующего» → дефис с пробелами вместо тире. Исправить: «маленький --- для следующего». **(1/3, перепроверено)**

3. **[строка 202]** «в пакете `dplyr`» → по §9 должно быть `{dplyr}`. **(2/3)**

4. **[строка 189]** «в параметрах `yes =` и `no =`» → по §8, при вызове функции — «аргументах». **(2/3)**

5. **[строка 211]** «с помощью параметра `.default =`» → по §8: «аргумента». **(2/3)**

6. **[строка 113]** «осуществляется по одинаковой формуле» → слово-паразит (§1). Лучше: «вычисляется по одинаковой формуле». **(2/3)**

7. **Нет вводного абзаца** — глава начинается сразу с `##` без общего введения. По §6 нужен абзац перед первым подразделом. **(3/3)**

8. **Нет итогового абзаца** — глава заканчивается техническим примером без подведения итогов. **(3/3)**

## 🔵 Желательные

1. **[строка 124]** Заголовок callout `## *Полезное:* зачем циклы? прост` — «прост» выглядит как сленговое сокращение; может быть непонятно части аудитории. **(2/3)**

2. **[строка 230]** «Чтобы на место `NA` поставить NA» → второе NA без бэктиков. Исправить: «поставить `NA`». **(1/3, перепроверено)**

3. **[строка 202]** «которая работает с использованием формулы» → в учебнике по статистике «формула» двусмысленна; лучше «с использованием синтаксиса тильды (`~`)». **(1/3, перепроверено)**

4. **[строка 242, 251]** Одна и та же ссылка на блогпост повторяется дважды с текстом-характеристикой. Рекомендуется оставить одну, вторую сделать описательной: «[сравнение подходов по скорости](...)». **(1/3, перепроверено)**

## Резюме

Глава компактная и хорошо структурирована по содержанию: логично проводит читателя от невекторизованного `if/else` через `for` к векторизованным `ifelse()` и `case_when()`. Авторский голос живой и уместный. Основные приоритеты на доработку: (1) исправить 5 случаев `--` → `---` и незакрытую скобку на строке 184 — это механические ошибки; (2) добавить вводный и итоговый абзацы по §6; (3) привести терминологию «параметр/аргумент» в соответствие с §8.
