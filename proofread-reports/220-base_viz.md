# Отчёт вычитки: 220-base_viz.qmd
Дата: 2026-06-01

## Механические проверки (bash)

- **TODO-комментарии автора:** строка 33 (`<!--# КАРТИНКА С ИРИСАМИ??? -->`), строка 114 (`<!--# Картинка с боксплотом -->`)
- **Двойное тире вместо em dash (`--` → `---`):** строки 25, 31, 60, 84, 89, 95, 108, 110, 116, 120 — массовое нарушение §3
- **Дефис вместо тире:** строка 23 (` - это тоже`), строка 106 (`а справа - "предикторы"`)
- **Cross-references на внешние секции:** `@sec-multivar`, `@sec-summary` — не определены в файле (ожидаемо, это ссылки на другие главы, проверить что якоря существуют в тех главах)
- **library() и read_csv():** подавление message/warning присутствует (строки 10–11) ✓

## 🔴 Критичные

1. **Опечатка: «интервал» → «интервалов»** (строка 84) — `количество интервал очень ориентировочно` → `количество интервалов`. **(3/3)**

2. **Пропущен пробел перед скобкой** (строка 23) — `` `summary()`(см. @sec-summary) `` → `` `summary()` (см. @sec-summary) ``. **(2/3)**

3. **TODO-комментарии не закрыты** (строки 33, 114) — два плейсхолдера для картинок остались в тексте. Решить: добавить картинки или удалить комментарии. **(3/3, bash)**

4. **Нежелательный формат `***` (вложенный жирный+курсив) для терминов** — нарушение §7 в 5 местах. **(3/3)**
   - Строка 21: `**диаграмма рассеяния (точечная диаграмма, *scatterplot),***` → `**диаграмма рассеяния** (точечная диаграмма, *scatterplot*)`
   - Строка 23: `**универсальная *(generic)*** функция` → `**универсальная** *(generic)* функция`
   - Строка 95: `**правило Фридмана-Диакониса *(Freedman--Diaconis' rule)***` → `**правило Фридмана-Диакониса** *(Freedman–Diaconis' rule)*`
   - Строка 95: `**правило Скотта *(Scott's normal reference rule).***` → `**правило Скотта** *(Scott's normal reference rule)*`
   - Строка 100: `**ящик с усами *(boxplot with whiskers):***` → `**ящик с усами** *(boxplot with whiskers)*:`

5. **Термин «гистограмма» — перевод не в курсиве** (строка 51) — `**гистограмма (histogram)**` → `**гистограмма** *(histogram)*`. **(3/3)**

6. **Массовое `--` вместо `---`** (§3) — 10+ вхождений по всему файлу: строки 25, 31, 60, 84, 89, 95, 108, 110, 116, 120. Заменить все ` -- ` на ` --- `. **(3/3, bash)**

7. **Дефис вместо тире** (строка 23, строка 106) — ` - это тоже` и `а справа - "предикторы"` → заменить на ` --- `. **(2/3, bash)**

## 🟡 Важные

1. **Термин «межквартильный размах» не введён формально** (строка 110) — при первом упоминании нужно: `**межквартильный размах** *(interquartile range, IQR)*`. Аббревиатура IQR используется в строке 112 без определения. **(2/3)**

2. **Повторное жирное для «правило Стёрджеса»** (строка 95) — первое введение в строке 89 (в callout), повторное жирное в строке 95. По §7 убрать `**` при повторе. **(1/3, перепроверено — подтверждаю)**

3. **Прямые кавычки вместо ёлочек** (строка 106) — `"предикторы"` → `«предикторы»`. **(2/3)**

4. **Нет переноса строки после display-формулы** (строка 89) — `$$...$$Здесь` — формула и текст слиплись. Добавить пустую строку после `$$`. **(1/3, перепроверено — подтверждаю, Quarto может некорректно отрендерить)**

5. **Формула и класс формул без cross-reference** (строка 106) — `уже знакомый нам класс формул` — нет ссылки на раздел, где он вводился. Добавить `(см. @sec-...)`. **(2/3)**

6. **Q1, Q3 используются без введения** (строка 110) — понятия квартилей не определены в этой главе. Если определены в 210-desc_stats.qmd, добавить cross-reference. **(1/3, перепроверено — подтверждаю, квартили должны быть определены ранее)**

7. **Двойной «известн-» в сноске** (строка 25) — `известным ботаником Эдгаром Андерсоном, но стал известен благодаря` — повтор корня в одном предложении. **(2/3)**

8. **«область математики» про статистику** (строка 116) — `в статистике, области математики` — статистика не раздел математики в строгом смысле. Рекомендация: убрать уточнение или заменить на `математической дисциплине`. **(1/3, перепроверено — пограничный случай, но для точности стоит поправить)**

## 🔵 Желательные

1. **Текст ссылки пограничный** (строка 120) — `[более чем достаточно]` — формально работает в контексте предложения, но можно сделать информативнее: `[сравнении ggplot2 и base R](...)`. **(2/3)**

2. **Повтор «просто» в одном предложении** (строка 116) — `Просто так решили... просто потому что`. **(1/3)**

3. **Повтор «число» в одном предложении** (строка 116) — `число 1.5 -- число более красивое`. **(1/3)**

4. **Чанки с графиками без label и fig-cap** — чанки plot/hist/boxplot не имеют `#| label: fig-...` и `#| fig-cap:`. Для cross-referencing и accessibility стоит добавить. **(1/3)**

5. **Статья Фишера 1936 года упомянута без `[@key]`** (строка 25, сноска) — академический источник в виде пересказа, формально не гиперссылка, но можно добавить в bibliography. **(1/3)**

## Резюме

Глава короткая, хорошо структурированная, с ярким авторским голосом и удачными объяснениями (особенно разбор боксплота). Главные приоритеты на доработку: (1) массовая замена `--` на `---` — тривиальная, но затрагивает 10+ строк; (2) исправление формата терминов `***` → раздельные `**` и `*` в 5 местах; (3) опечатка «интервал» → «интервалов» и слипшаяся формула с текстом. Два TODO-комментария с картинками требуют решения автора.
