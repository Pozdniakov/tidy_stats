# Отчёт вычитки: 030-import_data.qmd
Дата: 2026-05-31 (дополнено 2026-06-03)

## Механические проверки (bash)

- **Двойное тире вместо em dash:** строки 250, 256 (`--` вместо `---`)
- **Изображения без alt-текста:** строки 29, 264
- **Якорь без `sec-` префикса:** строка 246 (`{#import_googlesheets}`)
- **Cross-reference `@sec-rmd`:** используется, но определён в другом файле (не битая — внешняя ссылка)
- **Устаревший синтаксис чанков:** строки 13, 23, 35, 43, 252, 258 (` ```{r, ...} ` вместо `#| key: value`)
- **Чанки с `read_csv()` / `vroom()` без `warning: false`:** строки 279, 286 (первый `vroom` — намеренно)
- **Hunspell:** не установлен в среде

## 🔴 Критичные

1. **[строка 49] Фактическая ошибка о слэшах в Windows** (1/3 — Opus, перепроверено: подтверждено)
   `знак `/` является особым знаком для R, поэтому вместо него нужно использовать двойной `//``
   → В Windows проблема с `\` (обратный слэш), не `/`. Замена — `\\` или `/`. Текст говорит ровно наоборот.
   → Исправить: «знак `\` является особым знаком для R, поэтому вместо него нужно использовать двойной `\\` или прямой `/`»

2. **[строка 11] Падежная ошибка** (3/3)
   `мы к этой функцией очень скоро перейдем` → «к этой функции»

3. **[строка 129] Орфографическая ошибка** (3/3)
   `Если у вас получится с этим справится` → «справиться» (инфинитив)

4. **[строка 197] Опечатка** (3/3)
   `неправильно подобран разделить колонок` → «разделитель»

5. **[строка 289] Опечатка** (3/3)
   `означет` → «означает»

6. **[строка 246] Якорь без `sec-`** (3/3)
   `{#import_googlesheets}` → `{#sec-import_googlesheets}`

7. **[строки 29, 264] Изображения без alt-текста** (3/3)
   `![](images/030-import_data_console_wd.png)` и `![](images/030-import_data_google_choose.png)` → добавить описательный alt-текст

8. **[строка 3] Грамматическая ошибка** (1/3 — Opus, перепроверено: подтверждено)
   `каждая из которой имеет свой тип` → «каждая из которых»

9. **[строка 135] Согласование** (1/3 — Sonnet, перепроверено: подтверждено)
   `с другими параметрами по умолчанию, соответствующие формату` → «соответствующими»

10. **[строка 161] Падежная ошибка** (1/3 — Sonnet, перепроверено: подтверждено)
    `обратите внимания` → «обратите внимание»

11. **[строка 143] Орфография** (1/3 — Opus, перепроверено: подтверждено)
    `тоже самое` → «то же самое» (раздельно)

## 🟡 Важные

1. **Дефис вместо em dash — массовый дефект** (3/3)
   Строки 3, 129, 139, 141, 143 и др.: ` - ` → ` --- `
   Строки 250, 256: ` -- ` → ` --- `

2. **«Параметр» вместо «аргумент» — массовый дефект** (2/3 — Sonnet, Opus)
   Строки 135, 145, 152, 177, 186, 188, 197, 203: «параметр» в контексте R-функций → «аргумент» (§8)

3. **[строка 3] Термин «датасет» без оформления** (2/3 — Opus+Haiku)
   `датасета (так мы будем называть любой набор данных)` — при вводе нового термина формат: **датасет** *(dataset)* (§7).

4. **[строка 119] Горячие клавиши без бэктиков** (2/3 — Opus+Haiku)
   `горячие клавиши Cmd + Shift + R` → `` `Cmd` + `Shift` + `R` `` (и добавить вариант для Windows: `` `Ctrl` + `Shift` + `R` ``), §13.

5. **Пакеты без фигурных скобок — массовый дефект** (3/3)
   `readxl`, `xlsx`, `openxlsx`, `haven`, `foreign` (строки 242–244), `googlesheets4` (250), `readr` (276, 297), `vroom` (283, 297), `data.table` (289, 297) → обернуть в `{...}`

4. **Устаревший синтаксис чанков** (2/3 — Sonnet, Opus)
   Строки 13, 23, 35, 43, 252, 258: ` ```{r, eval = FALSE} ` → `#| eval: false`

5. **[строка 57] «Вашем» с заглавной** (3/3)
   `не только на Вашем компьютере` → «вашем»

6. **[строка 9] Неинформативный текст ссылки** (3/3)
   `скачайте файл по [ссылке](...)` → `скачайте [файл heroes_information.csv](...)`

7. **[строка 195] Неинформативный текст ссылки** (2/3 — Sonnet, Opus)
   `попробуйте [разобрать](...)` → `воспользуйтесь [декодером Лебедева](...)`

8. **Отсутствует итоговый абзац** (3/3)
   Глава обрывается на техническом сравнении (строка 308). Нужен завершающий абзац по §6.

9. **[строка 11] Лексическая ошибка: «разрешение» → «расширение»** (2/3 — Opus+Haiku, перепроверено: подтверждено)
   `указав его имя и разрешение` / `название файла с разрешением` — расширение файла по-русски «расширение» (extension), не «разрешение» (permission/resolution). Смысловая ошибка встречается дважды: строки 11 и 21.

10. **[строка 97] Оборванное предложение** (1/3 — Opus, перепроверено: подтверждено)
    `Результаты работы, например, отчеты, сгенерированные с помощью *Quarto* (см. @sec-rmd).` — нет сказуемого, предложение не закончено. Вариант: «…сгенерированные с помощью *Quarto*, стоит держать в отдельной папке».

11. **[строка 238] Согласование «с этим файлами»** (1/3 — Opus, перепроверено: подтверждено)
    `работать с этим файлами` → «с этими файлами».

12. **[строка 274] Согласование «с миллионами строчками»** (1/3 — Opus, перепроверено: подтверждено)
    `таблицу с миллионами строчками` → «с миллионами строчек» (род. пад. мн. ч.).

13. **[строка 88] Опечатка «с формат»** (1/3 — Opus, перепроверено: подтверждено)
    `основной скрипт с формат .R` → «в формате .R».

14. **[строка 256] Функция без `package::`** (1/3 — Opus, перепроверено: подтверждено)
   `read_sheet()` в тексте → `googlesheets4::read_sheet()` (первое упоминание, пакет не загружен)

10. **[строки 49, 67] Blockquotes вместо callout** (1/3 — Opus, перепроверено: обоснованно)
    Строка 49: предупреждение о Windows → `callout-important` с `*Осторожно:*`
    Строка 67: полезный совет → `callout-tip` с `*Полезное:*`

## 🔵 Желательные

1. **[строка 65] Длинный абзац** (2/3 — Sonnet, Opus)
   Абзац о проектах RStudio (~80 слов в одном предложении) — разбить на 2–3 предложения.

2. **[строка 135] Слово-паразит «данном»** (1/3 — Sonnet)
   `В данном случае используется формат` → «Здесь используется формат»

3. **[строка 137] Начало предложения** (2/3 — Sonnet, Haiku)
   `вы можете вручную создать такие файлы` → начало предложения после точки, строчная «в» → заглавная, или переформулировать.

4. **[строка 39] Текст ссылки** (2/3 — Sonnet, Opus)
   `[не рекомендуется](...)` → лучше с указанием источника: `[Дженни Брайан объясняет, почему](...)` или `[не рекомендуется (Tidyverse Blog)](...)`

## Резюме

Глава хорошо структурирована, последовательно ведёт от проблемы поиска файла к различным способам импорта и экспорта. Авторский голос живой и дружелюбный, метафора с Магометом и горой — удачная мнемоника. Самый критичный дефект — фактическая ошибка в описании слэшей для Windows (строка 49), где `/` и `\` перепутаны. Два массовых дефекта требуют сквозной правки: замена дефисов на em dash и «параметр» → «аргумент». Также нужен итоговый абзац главы.

---

## Решения корректора (сессия 2026-06-05)

✅ применено · ❌ отклонено · ⏸️ отложено · ✏️ вариант. Главу применяли интерактивно с нуля (cowork не трогал содержательные пункты отчёта).

| Место | Что предложено | Решение |
|---|---|---|
| стр. 49 слэши Windows | Факт. ошибка: спецсимвол `\`, не `/` | ✅ переписано, **проверено запуском в R** (escape `\`, `/` работает на всех ОС) |
| 13 опечаток/грамматики (батч) | которой→которых, функцией→функции, разрешение→расширение ×2, с формат→в формате, справится→справиться, соответствующие→соответствующими, тоже самое→то же самое, внимания→внимание, разделить→разделитель, этим файлами→этими, строчками→строчек, означет→означает | ✅ |
| стр. 246 якорь | `{#import_googlesheets}` → `{#sec-import_googlesheets}` | ✅ |
| тире (sed) | ` -- `/` - ` → ` --- `, кроме пути по меню `Tool - Global Options...` (стр. 67) | ✅ + добавлено исключение в CONVENTIONS §3 |
| стр. 3 «датасет» | Выделить термином **датасет** *(dataset)* | ❌ не выделять (первое появление в 010; общеупотребительное слово, как «среднее») |
| стр. 57 «Вашем»; стр. 137 «вы» после точки | вашем; Вы | ✅ |
| **§8 «параметр»→«аргумент» (батч)** | Заменить все «параметр» на «аргумент» | ❌ **ОТМЕНЕНО** после глубокого исследования (см. `research-notes.md`): R зовёт слот «формальным аргументом», CS — «параметром»; решено оставить «параметр». §8 переписан, в 010 добавлен callout «Для продвинутых», заведён аудит (задача #4) |
| §9 пакеты (батч) | readr/vroom/data.table/readxl/haven/foreign/googlesheets4/xlsx/openxlsx → `{}` со ссылками; «является»→«входит» (242); tidyverse-экосистема → курсив | ✅ |
| чанки | 6× старый синтаксис → `#\|`; `#\| warning: false` для `readr::read_csv`; `vroom` (286) — намеренное исключение | ✅ |
| стр. 9, 199 ссылки | «[ссылке]»→«[файл с данными…]», «[разобрать]»→«[определить кодировку]» | ✅ |
| стр. 261 `read_sheet()` | → `googlesheets4::read_sheet()` (§10) | ✅ |
| стр. 123 горячие клавиши | `Cmd + Shift + R` → `` `Ctrl` + `Shift` + `R` (`Cmd`…macOS) `` | ✅ |
| стр. 101 оборванное предложение | Дать сказуемое | ✏️ «…удобно складывать в отдельную папку для выходных данных» (антецедент для «туда же», без повтора) |
| §1 «данный» (139, 158, 207) | «В данном случае»→«Здесь» ×2; «по данному параметру»→«по этому» | ✅ |
| формат *.csv* (139, 143, 147) | Пунктуация вне курсива; `***.csv***` вместо битого `***.csv**,*`; «Разделенные Запятыми»→строчными | ✅ |
| 2 изображения | alt-текст (§23) | ✅ |
| Итоговый абзац главы (§6) | Написать | ⏸️ отложено (контент) |
