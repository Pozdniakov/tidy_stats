# Отчёт вычитки: 310-infer_stats.qmd
Дата: 2026-06-02

## Механические проверки (bash)

- **TODO-комментарий** на строке 47: `<!--# Здесь картинку с интервалом! -->`
- **Двойное тире `--` вместо em dash `---`**: строки 11, 15, 21, 22, 23, 51, 106, 184, 422, 424, 426 (массовое)
- **Дефис ` - ` вместо em dash**: строки 5 (×3), 7, 33, 114, 122, 132, 141, 155, 270, 272, 307, 383, 389 (массовое)
- **7 изображений без alt-текста**: строки 29, 39, 248, 293, 311, 385, 436
- **Заглавное расширение**: `rdists.PNG` (строка 29) — должно быть `.png`
- **Битая cross-reference**: `@sec-base_vis_boxplot` используется, но не определена в файле
- **Сноска `[^isnotnormal]`** не соответствует формату `[^310-infer_stats-N]`
- **library() без message: false**: строка 348 — но `message: false` есть на строке 347 ✓

## 🔴 Критичные

1. **(3/3) Массовая замена тире.** `--` и ` - ` используются вместо `---` по всей главе (~25 случаев). Рекомендуется `sed -i 's/ -- / --- /g; s/ - / --- /g'` + ручная проверка.

2. **(3/3) [строка 381]** `вы про него точно слышали` → строчная «в» после точки. Исправить: `Вы` (начало предложения, не нарушение правила о строчном «вы»).

3. **(2/3) [строки 136–137]** Сломанная сноска `\[\^isnotnormal\]` — экранированные скобки, Quarto не обработает. Исправить: `[^isnotnormal]` (или `[^310-infer_stats-N]`). Также `\[\^isnotnormal\]:` на строке 137.

4. **(2/3) [строка 137]** `апроксимироваться` → `аппроксимироваться`.

5. **(3/3) [строка 37]** `колоколообразнуюю` → `колоколообразную`.

6. **(3/3) [строка 108]** `свзязано` → `связано`. Также `числа дробные числа` → убрать первое «числа».

7. **(2/3) [строка 170]** `счиnаются` → `считаются` (латинская n в русском слове).

8. **(3/3) [строка 272]** `Пределельная` → `Предельная`.

9. **(3/3) [строка 282]** `ассиметрично` → `асимметрично`.

10. **(3/3) [строка 155]** `тем в ближе` → `тем ближе`.

11. **(2/3) [строка 194]** `выборочным распределение` → `выборочным распределением`.

12. **(2/3) [строка 344]** `в центральной совокупности` → `в генеральной совокупности`.

13. **(3/3) [строка 299]** `callout-note` с заголовком `*Полезное:*` → должен быть `callout-tip`.

14. **(3/3) [строки 29, 39, 248, 293, 311, 385, 436]** 7 изображений без alt-текста (§23).

15. **(2/3) [строки 473, 477]** `.02` и `.05` без нуля перед точкой → `0.02`, `0.05` (§14).

16. **(2/3) [строка 444]** `вычетанием` → `вычитанием`.

17. **(2/3) [строка 264]** Опечатка в якоре `{#sec-nordm_dist_importance}` → `{#sec-norm_dist_importance}`.

18. **(1/1 bash) [строка 47]** TODO-комментарий автора: `<!--# Здесь картинку с интервалом! -->`.

19. **(3/3) [строка 11]** Пропущена запятая: `переменная значения которой` → `переменная, значения которой`.

20. **(2/3) [строка 473]** `принимаем $H_1$` → статистически неточно. Лучше: `отклоняем $H_0$ в пользу $H_1$`.

## 🟡 Важные

1. **(3/3)** Массовая проблема с форматом терминов: двойные жирные блоки `**термин** **(*перевод*).**` на строках 15, 21, 22, 23, 132, 165, 230, 260. Нужно объединить: `**термин** *(перевод)*`.

2. **(3/3)** Формат `***` (жирный курсив) на строках 33, 41, 155, 260, 383 — нежелательный формат. Исправить на `**термин** *(перевод)*`.

3. **(2/3) [строка 51]** «основные параметры» функции → «аргументы» (§8). Также строки 114, 446.

4. **(2/3) [строка 11]** Терминологическая непоследовательность: введено «случайная величина», но далее чередуется с «случайная переменная» (строки 13, 15, 45, 106). Унифицировать.

5. **(2/3) [строка 29]** Расширение `.PNG` → `.png` (§23, переименовать файл).

6. **(2/3)** Имена файлов изображений не следуют конвенции `NNN-chaptername_description.ext`: `rdists.PNG`, `normalstandard.jpg`, `IllustrationCentralTheorem.png`, `explanationsPValue.png`.

7. **(2/3) [строка 173]** Лишнее «то»: `Чтобы получить... то нам нужно` → убрать «то».

8. **(2/3) [строка 33]** Пропущена запятая: `описать нормальное распределение нам нужно` → `описать нормальное распределение, нам нужно`.

9. **(2/3) [строка 7]** Нотация $m$, $s$ для выборочных статистик непоследовательна с дальнейшим использованием $\hat{\mu}$, $\bar{x}$ в главе.

10. **(2/3) [строка 344]** `ggplot2` без фигурных скобок → `{ggplot2}` (§9).

11. **(2/3)** Глава обрывается на таблице (строка 485) без итогового абзаца (§6).

12. **(2/3) [строка 401]** Метафора «доказательство от обратного» для NHST — упрощение, которое может создать неверное понимание (в математике противоречие строгое, в NHST — вероятностное). Рекомендуется оговорка.

13. **(1/1 bash)** Битая cross-reference `@sec-base_vis_boxplot` — якорь не определён в этом файле (вероятно, из другой главы — проверить).

## 🔵 Желательные

1. **(2/3) [строка 260]** Повторное жирное «стандартной ошибки среднего» — термин уже введён на строке 230.

2. **(2/3) [строка 122]** Длинное пояснение о `set.seed()` в blockquote (~10 строк) — лучше подходит для `callout-tip`.

3. **(2/3) [строка 134]** Абзац почти дословно повторяет строку 33 (два параметра нормального распределения).

4. **(1/3) [строка 143]** «самой лучшей оценкой» без обоснования — можно добавить «несмещённой».

5. **(2/3) [строка 409]** $N$ (заглавное) в формуле z-статистики, но в коде и остальных формулах используется $n$. Унифицировать.

6. **(1/3) [строка 137]** Имя сноски `[^isnotnormal]` не соответствует формату `[^310-infer_stats-N]`.

## Резюме

Глава «Статистика вывода» содержит отличное педагогическое изложение сложных концепций (выборочные распределения, ЦПТ, NHST) с удачными визуализациями и интерактивными демонстрациями. Статистически значимых ошибок в объяснениях почти нет — определение p-value корректное, интерпретация доверительного интервала правильная.

Главные приоритеты на доработку: (1) массовая замена тире — ~25 случаев ` -- ` и ` - ` вместо ` --- `, решается одним sed; (2) исправление ~10 опечаток, включая сломанную сноску `\[\^isnotnormal\]`; (3) добавление alt-текста к 7 изображениям и приведение формата терминов к единообразному виду `**термин** *(перевод)*`.
