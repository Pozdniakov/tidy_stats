# Отчёт вычитки: 013-vector.qmd
Дата: 2026-05-30

Примечание: Haiku-субагент не запустился (rate limit), синтез по 2 субагентам (Sonnet + Opus).

## Механические проверки (bash)

- **` -- ` вместо em dash:** ~22 вхождения по всему файлу
- **Изображения без alt-текста:** 6 штук (строки 9, 13, 119, 253, 388, 530)
- **Старый синтаксис чанков:** строки 30, 491
- **Cross-references:** @sec-about, @sec-for, @sec-apply, @sec-type_logical — ссылки на другие главы, ОК

## 🔴 Критичные

1. **(2/2) [строка 60]** `второй агрумент` → опечатка: «аргумент»

2. **(2/2) [строка 30]** `` ```{r error=TRUE} `` → старый синтаксис чанка (§17). Мигрировать на `#| error: true`

3. **(2/2) [строка 491]** `` ```{r, eval = FALSE} `` → старый синтаксис (§17). Мигрировать на `#| eval: false`

4. **(2/2) [строка 376]** `### оператор %in% и match()` → заголовок со строчной буквы (§18). Исправить: `### Оператор %in% и match()`

5. **(2/2) [строка 42]** `с шагом 1. вы не представляете` → «вы» со строчной после точки — грамматическая ошибка. Исправить: «Вы не представляете»

6. **(2/2) [строка 413]** `так можно заменить все значения кроме выбранных заменить на NA` → слово «заменить» дублируется. Исправить: убрать первое «заменить»

## 🟡 Важные

1. **(2/2) Массовая замена ` -- ` → ` --- `** — ~22 вхождения. Рекомендуется sed с ручной проверкой. Также строка 422 и 502: ` - ` вместо ` --- `

2. **(2/2) [строки 67, 407, 489, 495]** «параметр» вместо «аргумент» в контексте R-функций (§8)

3. **(2/2) [строки 5, 528]** Формат `***term***` (жирный курсив) нежелателен по §7. Исправить на `**термин** *(перевод)*`

4. **(2/2) [строка 489]** `функции *mean()*` → функция курсивом вместо бэктиков. Исправить: `` `mean()` ``

5. **(2/2) [строка 334]** `долю выраженную в процентах` → пропущена запятая: `долю, выраженную в процентах`

6. **(1/2, Opus) [строки 162, 178, 218, 279]** Blockquotes используются для предупреждений и важных советов — по §20 лучше оформить как callout-блоки (callout-important или callout-tip)

7. **(2/2) [строка 267]** `my_named_vector['first']` → одинарные кавычки в R-коде; по §15 — двойные: `"first"`

8. **(2/2) [строка 182]** `правило **ресайклинга** (*правило переписывания, recycling rule*)` → формат термина с переводом нарушает §7. Исправить: `**ресайклинг** *(recycling rule)*`

## 🔵 Желательные

1. **(2/2)** 6 изображений без alt-текста (строки 9, 13, 119, 253, 388, 530)

2. **(1/2) [строка 402]** `match()` в заголовке callout без бэктиков → добавить бэктики

3. **(2/2) [строка 495]** `вы знаете, что нужно делать!` → «вы» после точки — «Вы»

## Резюме

Глава логично структурирована, с хорошим вводным и итоговым абзацами, понятными примерами и последовательным изложением. Главные приоритеты: (1) массовая замена ` -- ` на ` --- ` (~22 вхождения); (2) замена «параметр» на «аргумент» в 4 местах; (3) исправление старого синтаксиса чанков (2 места) и опечатки «агрумент». Стоит также рассмотреть перевод нескольких blockquotes в callout-блоки для единообразия.
