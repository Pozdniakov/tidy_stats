# Отчёт вычитки: 105-tidyverse.qmd
Дата: 2026-05-31

## Механические проверки (bash)

- **Двойное тире:** строка 5 — ` -- ` вместо ` --- `
- **Cross-references без определения в файле:** `@sec-beyond_base_r`, `@sec-tidy_intro`, `@sec-tidyverse_advanced` — ожидаемо, определены в других главах
- **Мёртвый якорь:** `{#sec-whole_tidyverse}` — определён, но не используется в этом файле (может использоваться из других глав)
- Остальные проверки (TODO, слова-паразиты, код, Quarto-структура) — чисто

## 🔴 Критичные

1. **[строка 7]** «аггрегации» → опечатка, правильно «агрегации» (одна «г»). **(2/3)**

## 🟡 Важные

1. **[строка 5]** ` -- ` → ` --- ` (двойное тире вместо длинного тире, нарушение §3). **(3/3)**
2. **[строка 9]** «пакету purrr» → `{purrr}` — пакет без фигурных скобок и бэктиков (нарушение §9). **(3/3)**
3. **[строка 5]** `` `{data.table}` `` — нет ссылки при первом упоминании (§9 требует ссылку). **(2/3)**

## 🔵 Желательные

1. **[строка 3]** «коллекцию пакетов tidyverse» — tidyverse не оформлен как `` `{tidyverse}` `` (§9). Формально нарушение, но в контексте экосистемы допустимо. **(1/3, подтверждено при перепроверке)**

## Резюме

Файл — короткое оглавление раздела «Tidyverse» (9 строк), поэтому находок немного. Основные проблемы: опечатка «аггрегации», двойное тире вместо длинного, и неоформленный пакет `purrr`. Все исправления тривиальны и займут пару минут. Содержательно текст корректен и хорошо структурирован.
