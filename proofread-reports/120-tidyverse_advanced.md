# Отчёт вычитки: 120-tidyverse_advanced.qmd
Дата: 2026-06-01

## Механические проверки (bash)

- **Двойное тире вместо em dash:** 9 вхождений (строки 134, 165, 167, 256, 329, 345, 400, 450, 465)
- **Чанк с `library()` без `message: false`:** строка 10
- **Чанк с `read_csv()` без `warning: false`:** строка 11
- **Нет подавления `message`/`warning`:** ни одного `message: false` или `warning: false` в файле
- **Изображение без alt-текста:** строка 93
- **Ссылка с неинформативным текстом:** не найдено bash (но найдено субагентами на строке 199)
- **Cross-references — использованы, но не определены в файле:** `@sec-anon_f`, `@sec-apply_f`, `@sec-functions_objects`, `@sec-purrr`, `@sec-tidyselect` (это ссылки на другие главы — допустимо, не битые)
- **Орфография:** hunspell не установлен

## 🔴 Критичные

1. **[строка 102]** `«взятых их `y`»` → опечатка: «их» вместо «из». Исправить на «взятых из `y`». **(2/3: Sonnet, Opus)**

2. **[строка 102]** `«соотвествия»` → опечатка: пропущена буква «в». Исправить на «соответствия». **(3/3)**

3. **[строка 111]** `«поставить соответстие»` → опечатка: «соответстие» → «соответствие». **(2/3: Sonnet, Opus)**

4. **[строка 216]** `«Превратим тиббл `new_diet` длинный:»` → пропущен предлог «в». Исправить: «Превратим тиббл `new_diet` в длинный:». **(3/3)**

5. **[строка 225]** `«А теперь обратно в короткий:»` → «короткий» вместо «широкий» — несогласованность с терминологией главы. Исправить на «в широкий». **(3/3)**

6. **[строка 238]** `«использую запятую»` → опечатка: «использую» → «используя». **(2/3: Sonnet, Opus)**

7. **[строка 93]** `![](images/120-tidyverse_advanced_joins.png)` → изображение без alt-текста. Добавить описание: `![Схема работы функций *_join()](images/...)`. **(3/3)**

8. **[строка 9–13]** Чанк с `library()` без `#| message: false` и чанк с `read_csv()` без `#| warning: false`. Добавить YAML-опции. **(3/3 — bash + все субагенты)**

9. **[строка 256]** `«Первый аргумент `.col`»` → правильное имя аргумента `.cols` (с «s»). **(1/3: Opus, но перепроверено — действительно `.cols` в документации `across()`)**

10. **[строка 450]** `«в одной ячейки»` → ошибка падежа: «ячейки» → «ячейке». **(2/3: Sonnet, Opus)**

11. **[строка 465]** `«разобъем»` → опечатка: «разобъем» → «разобьём». **(1/3: Opus, но перепроверено — действительно ошибка: мягкий знак, не твёрдый)**

## 🟡 Важные

1. **[строки 134, 165, 167, 256, 329, 345, 400, 450, 465 и др.]** Двойное тире ` -- ` и ` - ` вместо em dash ` --- ` — ~15 вхождений по всей главе. **(3/3)**

2. **[строка 196]** `` ```{r, echo = FALSE, results = 'asis'} `` → старый синтаксис опций чанка. Мигрировать на `#|`. **(3/3)**

3. **[строка 345 и др.]** Пакеты `purrr`, `dplyr`, `broom` упоминаются без фигурных скобок (`{purrr}`, `{dplyr}`, `{broom}`). Около 8 вхождений. **(3/3)**

4. **[строка 87]** `«называется **ключом**»` → термин без английского перевода. По конвенции: **ключ** *(key)*. **(3/3)**

5. **[строка 402]** `«нестингом»` → термин используется без формального введения. Рекомендуется при первом использовании: **нестинг** *(nesting)*. **(2/3: Opus, Haiku)**

6. **[строка 104, 267]** `«параметром `by =`»`, `«параметр `na.rm =`»` → по конвенции это «аргумент», не «параметр». **(3/3)**

7. **[строка 163–165]** Формат `***tidy data***` и `**широком формате *(wide data)***` — вложенный курсив внутри жирного ломается в PDF/EPUB. Разделить: `**широкий формат** *(wide data)*`. **(3/3)**

8. **[строка 199]** `«по [ссылке](https://...)»` → текст ссылки неинформативен. Исправить: `[анимацию на GitHub](...)`. **(3/3)**

9. **[строка 1]** Глава не имеет вводного абзаца — сразу начинается подраздел. **(3/3)**

10. **[строка 500]** Глава заканчивается кодом без итогового абзаца. **(3/3)**

11. **[строка 402]** `«к использовании»` → ошибка падежа: «использовании» → «использованию». **(1/3: Opus, но перепроверено — действительно ошибка)**

12. **[строка 465]** `«исходный вектором»` → ошибка согласования: «исходный вектор» или «исходным вектором». **(1/3: Sonnet, перепроверено — действительно)**

## 🔵 Желательные

1. **[строка 375]** `map_df()` — функция устарела в `{purrr}` 1.0.0. Рекомендуется `list_rbind()` / `map_dfr()`. **(2/3: Sonnet, Opus)**

2. **[строка 477, 485]** `unnest()` без указания колонки — устаревший вызов, выдаст deprecation warning. Исправить на `unnest(genres)`. **(2/3: Sonnet, Opus)**

3. **[строка 491]** `separate_rows()` — deprecated в `{tidyr}` 1.3.0, заменена на `separate_longer_delim()`. **(1/3: Opus, перепроверено)**

4. **[строки 272, 281, 298, 312]** Передача аргументов через `...` в `across()` устарела с `dplyr` 1.1.0. Рекомендуется анонимная функция: `\(x) mean(x, na.rm = TRUE)`. **(1/3: Sonnet, перепроверено)**

5. **[строка 345]** `«В данном случае»` → слово-паразит «данный». Заменить на «В этом случае». **(2/3: Sonnet, Opus)**

6. **[строка 78]** `«обычно... обычно»` → повтор слова в одном предложении. Убрать одно. **(1/3: Opus, перепроверено)**

7. **[строка 91]** `«аргумента (`x =` и `y =`) в которые мы должны подставить два датафрейма/тиббла которые мы хотим»` → пропущены запятые перед обоими «которые». **(1/3: Sonnet, перепроверено)**

8. **[строка 366]** `«Есть функция `vapply()` в которой»` → пропущена запятая перед «в которой». **(1/3: Opus, перепроверено)**

9. **[строка 448]** Пакет `{broom}` упоминается без объяснения и без ссылки. **(1/3: Sonnet, перепроверено — уместен хотя бы краткий пояснительный комментарий)**

10. **[строка 167, 188]** Повторное жирное выделение уже введённых терминов «широкий/длинный формат». **(2/3: Opus, Haiku)**

11. **[строка 398]** Опечатка в якоре: `{#sec-list_colums_nest}` — «colums» вместо «columns». **(1/3: Haiku, перепроверено — технически не ломает ничего, но стоит исправить)**

## Резюме

Глава хорошо структурирована и последовательно объясняет сложные концепции tidyverse (join, pivot, across, purrr, nesting) с наглядными примерами. Авторский стиль дружелюбный и живой. Топ-3 приоритета на доработку: (1) исправить ~10 опечаток и ошибок согласования (критичные находки), включая «соотвествия», «использую», пропущенный предлог «в» и неверный термин «короткий» вместо «широкий»; (2) привести в порядок форматирование — заменить `--`/`-` на em dash, добавить `{}` к пакетам, исправить вложенный курсив в жирном; (3) добавить вводный и итоговый абзацы главы, а также обновить устаревшие вызовы (`map_df()`, `unnest()` без аргументов, `across()` с `...`).
