# Отчёт вычитки: 350-anova.qmd
Дата: 2026-06-02

## Механические проверки (bash)

- **TODO-комментарии:** нет
- **Двойные тире вместо em dash:** не обнаружены grep-ом (используется ` - ` — дефис с пробелами, не `--`)
- **`fig.cap=` (устаревший):** строка 89 — `fig.cap="\\label{fig:fig1}..."` 
- **`library()` без `message: false`:** строки 98 (`ggplot2`), 243 (`ez`), 381 (`apa`) — только строка 15 имеет подавление
- **`read_csv()` без `warning: false`:** строка 16
- **Битые cross-references:** `@sec-aova` (опечатка, должно быть `@sec-aov`?), `@sec-dep_ttest` (ссылка на другую главу — проверить)
- **Мёртвые якоря:** все определённые якоря не используются внутри файла (нормально, если на них ссылаются из других глав)
- **Hunspell:** не установлен

## 🔴 Критичные

1. **[строка 59]** `талбицу` → опечатка, должно быть «таблицу» **(3/3)**

2. **[строка 83]** `мы не делим вме в конце` → опечатка «вме», должно быть «всё» **(3/3)**

3. **[строка 114]** `мы всегда считаем считаем площадь` → дублирование «считаем» **(3/3)**

4. **[строка 334]** `внутригруппоdые` → латинская «d» вместо кириллической «в», должно быть «внутригрупповые» **(3/3)**

5. **[строка 372]** `с повторноми измерениями` → опечатка, должно быть «повторными» **(2/3: Sonnet, Opus)**

6. **[строка 374]** `по офромлению статей` → опечатка, должно быть «оформлению» **(3/3)**

7. **[строка 385]** `это будет выглядеть это будет вот так` → дублирование «это будет» **(3/3)**

8. **[строка 372]** `проверяет использует матрицу` → пропущено слово/знак. Вероятно: «проверяет и использует» или «проверяет, используя» **(2/3: Opus, перепроверено)**

9. **[строка 126]** `принимаем ненулевую (о том, что различия есть)` → некорректная формулировка: альтернативную гипотезу не «принимают». Правильно: «отвергаем нулевую гипотезу в пользу альтернативной» **(3/3)**

10. **[строка 253]** `мы не можем ее отбросить и поэтому принимаем` → (а) предложение обрывается без дополнения; (б) «принимаем нулевую гипотезу» — статистически некорректно, правильно «не можем отвергнуть». Сноска [^350-anova-5] частично оговаривает это, но основной текст формирует неверное представление **(3/3)**

11. **[строка 89]** `fig.cap="\\label{fig:fig1}..."` → устаревший синтаксис `fig.cap=` + LaTeX-метка `\label{}`. Мигрировать на `#| fig-cap:` и `#| label: fig-...` **(3/3)**

12. **[строка 7]** `@sec-aova` — битая cross-reference (опечатка в якоре). Вероятно, должно быть `@sec-aov` или другой существующий якорь **(bash)**

13. **[строка 206]** `Используем новую колонки` → грамматическая ошибка: «новую колонки» → «новые колонки» **(2/3: Sonnet, перепроверено)**

## 🟡 Важные

1. **Массовая замена ` - ` на ` — `:** по всему файлу дефис с пробелами вместо длинного тире (em dash). Затронуты строки 3, 7, 9, 81, 85, 124, 132, 134, 156, 162, 168, 200, 223, 240, 257, 270, 274, 344, 348, 352, 360, 368, 370, 372, 374, 389, 391. Рекомендуется массовая замена **(3/3)**

2. **[строки 248–249]** `detailed = T`, `return_aov = T` → `T` вместо `TRUE` в R-коде, нарушение §11/§15. Исправить на `TRUE` **(2/3: Sonnet, Opus)**

3. **[строка 257]** `return_aov = T` в прозе → `T` вместо `TRUE` **(2/3)**

4. **[строка 234]** `пакетом \`ez\`` → пакет без фигурных скобок (§9): должно быть `{ez}` **(3/3)**

5. **[строка 374]** `пакетом \`apa\`` → должно быть `{apa}` **(3/3)**

6. **[строка 389]** `пакеты \`apaStyle\` и \`papaja\`` → должно быть `{apaStyle}` и `{papaja}` **(2/3: Sonnet, Opus)**

7. **[строка 391]** `пакета: \`nlme\` и \`lme4\`` → должно быть `{nlme}` и `{lme4}` **(2/3: Sonnet, Opus)**

8. **[строка 186]** `**"фиктивное кодирование" (dummy coding):**` → формат не соответствует §7. Должно быть **фиктивное кодирование** *(dummy coding)* **(3/3)**

9. **[строка 136]** `параметра \`pool.sd()\`` → (а) «параметра» → «аргумента» (§8); (б) скобки `()` лишние — это аргумент, не функция **(3/3)**

10. **[строка 387]** `*p* = .007` → p-value без нуля перед точкой, нарушение §14. Должно быть `p = 0.007` **(3/3)**

11. **[строка 49]** `## Тестирование значимости нулевой гипотезы в ANOVA.` → точка в конце заголовка, нарушение §18 **(2/3: Sonnet, Opus)**

12. **[строка 7]** Blockquote используется для предупреждения/разъяснения → по §20 лучше заменить на callout-блок **(2/3: Sonnet, Opus)**

13. **[строки 236, 376, 380]** Старый синтаксис чанков `{r, eval = FALSE}`, `{r, message = FALSE}` → мигрировать на `#|` формат (§17) **(3/3)**

14. **[строка 391]** `очень популярный нынче метод` → начинается со строчной буквы после точки предыдущего предложения **(2/3: Opus, перепроверено)**

15. **[строка 298]** `Как видно по картинке` → нет cross-reference `@fig-label`, нарушение §22 **(2/3: Sonnet, Opus)**

16. **[строки 98, 243, 381]** `library(ggplot2)`, `library(ez)`, `library(apa)` → чанки без `#| message: false` **(bash)**

## 🔵 Желательные

1. **[строка 19]** «препроцессинг данных» → англицизм, лучше «подготовка данных» или «предобработка данных» **(2/3: Sonnet, Haiku)**

2. **[строка 130]** `post-hoc тесты (апостериорные тесты)` → термин вводится без жирного по §7 **(2/3: Sonnet, Opus)**

3. **[строка 47]** Цитата из `?aov` без указания источника → добавить `(из справки ?aov)` **(2/3: Sonnet, Haiku)**

4. **[строка 240]** `нужно указать, данные, зависимую переменную` → лишняя запятая после «указать» **(2/3: Sonnet, перепроверено)**

5. **[строка 372]** `с всего двумя условиями` → неуклюжее сочетание. Лучше «всего с двумя условиями» **(2/3: Opus, Haiku)**

6. **[строка 366]** Заключение перечисляет только пропущенные темы, но не резюмирует изученное **(2/3: Sonnet, Opus)**

7. **[строка 372]** Абзац о сферичности Моучли — ~95 слов одним предложением. Разбить на 2–3 **(2/3: Sonnet, Opus)**

8. **[строка 334]** «в нужных переменных: `between =` и `within =`» → «переменных» некорректно в контексте функции, лучше «аргументах» **(2/3: Opus, Haiku)**

## Резюме

Глава хорошо структурирована и последовательно ведёт читателя от однофакторного ANOVA к более сложным вариантам. Объяснение F-статистики через таблицу ANOVA и визуализацию понятное и доступное. Код рабочий и следует логике изложения.

Топ-3 приоритета: (1) исправить многочисленные опечатки (талбицу, вме, внутригруппоdые, повторноми, офромлению) и дублирования — их много для одной главы; (2) массовая замена дефисов ` - ` на длинные тире ` — ` — затронуто ~30 строк; (3) исправить статистически некорректные формулировки «принимаем гипотезу» (строки 126, 253) — для учебника по статистике это особенно важно.
