# Отчёт вычитки: 330-cov_cor

Дата: 2026-06-02

## Механические проверки (bash)

- **` -- ` вместо ` --- `**: массовый дефект — строки 56, 60, 102, 114, 184, 201, 284, 316 и другие (всего ~15 вхождений)
- **Дефис в диапазоне**: `2-3` (строка 229) — нужно en dash `2–3`
- **Чанки с `library()` без `message: false`**: строка 280 (`library(corrplot)`), строка 325 (`library(corrr)`)
- **Устаревший синтаксис чанков**: строки 5, 273, 320 — `` ```{r, eval = FALSE} `` вместо `#| eval: false`
- **Cross-references**: `@sec-dim_red_pca`, `@sec-quest_practices`, `@sec-var`, `@sec-z_scores` — использованы, но определены в других главах (нормально для книги, не ошибка)
- **Мёртвые якоря**: все якоря `##sec-*` определены, но ни один не используется внутри этой главы (могут использоваться из других глав — не ошибка)
- **Слова-паразиты / Вы с заглавной / TODO**: не обнаружены
- **hunspell**: не установлен в sandbox

## 🔴 Критичные

1. **[строка 38, 48]** `covariation` → `covariance`. Перевод термина неверный: *covariation* — явление совместного изменения, *covariance* — математический объект. Также *covariation matrix* → *covariance matrix*. **(3/3)**

2. **[строка 201]** `Это и будет ковариационной матрицей!` → Раздел посвящён **корреляционной** матрице (callout «Корреляционная матрица с точки зрения линейной алгебры»). Должно быть «корреляционной матрицей». **(2/3, подтверждено)**

3. **[строка 201]** `поделить результаты матрицу` → лишнее слово «результаты». Должно быть «поделить матрицу на $n - 1$». **(1/3, подтверждено при чтении)**

4. **[строка 316]** `статистические незначимые (с *p.value \< .05)` → Логическая ошибка: если крестиками отмечены **незначимые**, то условие должно быть `p > 0.05`, не `p < 0.05`. Также: «статистические» → «статистически»; `p.value` → `p-value`; `.05` → `0.05`. **(2/3, подтверждено)**

5. **[строка 241]** `групповую вероятность ошибки первого уровня` → ранее в тексте корректно «первого рода». «Первого уровня» — ошибка. **(1/3, подтверждено)**

6. **[строка 241]** `на на уровне` → дублирование предлога «на». **(2/3)**

7. **[строка 251]** `перечсиленные` → опечатка: «перечисленные». **(3/3)**

8. **[строка 127]** `изменения одной переменной не связано` → несогласованность: «изменения... не связаны» (мн.ч.). **(2/3)**

9. **[строка 26]** `в понятным для нас килограммах` → «в понятных для нас килограммах». **(3/3)**

10. **[строка 239]** `гарантирует контроль групповую вероятности` → «контроль групповой вероятности». **(1/3, подтверждено при чтении)**

11. **[строка 318]** Заголовок `## Матрица корреляций в виде графа` без якоря `{#sec-...}` — все остальные заголовки `##` имеют якоря. **(3/3)**

## 🟡 Важные

1. **[строки 56, 60, 102, 114, 184, 201, 284, 316 и др.]** ` -- ` → ` --- ` (em dash). Массовый дефект по всей главе, ~15 вхождений. **(3/3)**

2. **[строка 3]** `` `Stat2Data` `` → `` `{Stat2Data}` `` — пакет без фигурных скобок (§9). **(2/3)**

3. **[строка 215]** `` `psych` `` → `` `{psych}` `` — аналогично. **(2/3)**

4. **[строка 328]** `{corrr}` без бэктиков → `` `{corrr}` ``. **(2/3)**

5. **[строка 213]** `` `cor()`не `` → пропущен пробел между `` `cor()` `` и «не». **(2/3)**

6. **[строки 70, 156, 290, 336]** «параметр» / «параметры» в контексте функций R → «аргумент» / «аргументы» (§8). **(2/3)**

7. **[строки 5, 273, 320]** Устаревший синтаксис чанков `` ```{r, eval = FALSE} `` → `#| eval: false` (§17). **(2/3)**

8. **[строки 280, 325]** `library(corrplot)` и `library(corrr)` без `#| message: false` (§17). **(2/3)**

9. **[строка 163]** Blockquote `>` содержит важное методологическое предупреждение (выбор теста a posteriori). Должен быть `callout-important` (*Осторожно:*), не blockquote (§20). **(3/3)**

10. **[строка 163]** `в данном случае` → «данном» — слово-паразит (§1). Заменить на «в этом случае». **(2/3)**

11. **[строки 163, 213, 241, 251, 296]** `*p-value*` в курсиве → p-value не курсивится (§14). **(2/3)**

12. **[строка 118]** `**Коэффициент корреляции**` — первое введение без перевода *(correlation coefficient)*. Аналогично «Корреляцией» без *(correlation)*. **(2/3)**

13. **[строка 130]** `коэффициент корреляции Пирсона` — первое введение, не выделен жирным, нет перевода *(Pearson correlation coefficient)*. **(2/3)**

14. **[строка 38, 48]** Формат вложенного курсива в жирном: `**ковариация *(covariation).***` → должно быть `**ковариация** *(covariance)*`. **(2/3)**

15. **Отсутствует итоговый абзац** — глава заканчивается кодом без заключения (§6). **(3/3)**

16. **[строка 156]** `Кэнделла` → нестандартная транскрипция, общепринятая — «Кендалла» (Kendall). **(1/3, подтверждено)**

## 🔵 Желательные

1. **[строка 229]** `2-3` → `2–3` (en dash в диапазоне, §3). **(1/3, подтверждено bash)**

2. **[строка 60]** `по основным диагоналям` → «по основной диагонали» (ед.ч., диагональ одна). **(1/3, подтверждено)**

3. **[строка 227]** Сноска содержит прямые кавычки `"Проблема множественных сравнений"` → «ёлочки» (§2). **(2/3)**

4. **[строка 243]** `Бенджамини --- Хохберга` → em dash между фамилиями; в двойных фамилиях используется en dash. **(1/3, подтверждено)**

5. **[строка 303]** `разворошить полученную переменную mtcars_cor_p` → `mtcars_cor_p` без бэктиков; также отсутствует точка в конце. **(2/3)**

6. **[строка 209]** В проверочном коде используется `cov()` вместо `cor()`. Технически `cov()` на z-scores = `cor()` на оригинале, но для читателя `cor()` был бы яснее. **(1/3, подтверждено)**

7. **[строка 1–34]** Вводный абзац о целях главы размыт — глава начинается с загрузки данных, переход к теме корреляции только в строке 34. **(2/3)**

## Резюме

Глава хорошо структурирована и последовательно ведёт от ковариации через корреляцию Пирсона к непараметрическим коэффициентам, множественным сравнениям и визуализации. Авторский голос живой и доступный. Три главных приоритета на доработку: (1) массовая замена ` -- ` на ` --- ` по всей главе и исправление фактических ошибок (covariation → covariance, логическая ошибка в описании крестиков на хитмэпе, «ковариационной» → «корреляционной» в callout); (2) приведение форматирования в соответствие с конвенциями — фигурные скобки у пакетов, аргумент вместо параметр, p-value без курсива, новый синтаксис чанков; (3) добавление итогового абзаца и замена blockquote на callout-important для предупреждения о выборе теста a posteriori.
