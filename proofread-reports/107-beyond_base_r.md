# Отчёт вычитки: 107-beyond_base_r.qmd
Дата: 2026-06-01

## Механические проверки (bash)

- **`--` вместо `---`**: строки 11, 37, 69, 87, 95 — массовая замена нужна
- **`library()` без `message: false`**: строки 22, 84 — **намеренно** для этого файла
- **Cross-references**: `@sec-fastread` и `@sec-tidy_intro` используются, но определены в других главах — норма для Quarto-книги
- **Мёртвые якоря**: `#sec-beyond_base_r`, `#sec-dt_approach`, `#sec-dt_vs_tidy`, `#sec-tidy_approach` — определены, но не используются внутри файла (могут использоваться из других глав)
- Hunspell: не установлен в sandbox
- Остальные проверки: чисто

## 🔴 Критичные

1. **[строка 30]** `**дататейбл *(datatable):***` → Сломанное форматирование: вложенный курсив в жирный рендерится некорректно. Кроме того, "datatable" должно быть "data.table" (с точкой). Исправить на: `**дататейбл** *(data.table)*:` **(3/3)**

2. **[строка 37]** `**Дататейбл** -- это` → Повторное выделение жирным (§7 запрещает). Убрать жирный: `Дататейбл --- это`. **(2/3)**

## 🟡 Важные

1. **[строки 11, 37, 69, 87, 95]** ` -- ` → Двойное тире вместо длинного. Заменить все на ` --- ` (§3). **(3/3)**

2. **[строка 39]** `по-внимательнее` → Орфографическая ошибка. Правильно: `повнимательнее` (слитно). **(2/3)**

3. **[строка 95]** `**тиббл** -- "улучшенный" датафрейм` → Первое введение термина без перевода. Нужно: `**тиббл** *(tibble)* --- "улучшенный" датафрейм` (§7). **(2/3)**

4. **[строка 75]** `## Подход tidyverse` → *tidyverse* как подход/философия должен быть курсивом (§9): `## Подход *tidyverse*`. **(2/3)**

5. **[строка 122]** `## {data.table} vs tidyverse` → `{data.table}` без бэктиков, tidyverse без курсива. Исправить: `` ## `{data.table}` vs *tidyverse* ``. **(2/3)**

6. **[строка 126]** `используют в качестве основного инструмента tidyverse, но при работе с данными побольше переключаются на {data.table}` → tidyverse дважды без курсива. Аналогично в конце строки 126: `разработчики tidyverse`. Исправить на *tidyverse*. **(2/3)**

7. **[строка 87]** `(@sec-tidy_intro )` → Лишний пробел перед `)`. Исправить на `(@sec-tidy_intro)`. **(2/3)**

## 🔵 Желательные

1. **[строка 69]** `[бэнчмарк](...)` → Текст ссылки описывает жанр, а не содержимое (§24). Лучше: `[бенчмарк db-benchmark](...)`.

2. **[строка 7]** `очень сильно изменяют` → Усилительное «очень сильно» избыточно, можно заменить на «существенно изменяют».

3. **[строки 55, 114]** Двойное использование «Заметьте» — можно убрать одно для разнообразия.

4. **[строка 112]** `...с {data.table}), каждая отдельная операция имеет свою функцию.` → Comma splice: два самостоятельных предложения через запятую. Лучше точка с запятой или точка.

## Резюме

Глава короткая и хорошо структурированная — ясно вводит два подхода (`{data.table}` и *tidyverse*) с примерами кода и честным сравнением. Авторский голос живой и уместный. Топ-3 приоритета: (1) исправить сломанное форматирование термина «дататейбл» на строке 30, (2) массово заменить `--` на `---` (5 вхождений), (3) привести в порядок форматирование *tidyverse* в заголовках и прозе (строки 75, 122, 126).
