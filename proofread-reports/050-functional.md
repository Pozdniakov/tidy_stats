# Отчёт вычитки: 050-functional.qmd
Дата: 2026-05-31

## Механические проверки (bash)

- **Двойное тире:** строка 45 — ` -- ` вместо ` --- `
- **Битые cross-references:** `@sec-df`, `@sec-distributions`, `@sec-purrr`, `@sec-r_vis`, `@sec-tidy_across` — используются, но не определены в файле (предположительно определены в других главах — нужно убедиться, что метки совпадают)
- **Мёртвые якоря:** `#sec-anon_f`, `#sec-apply`, `#sec-apply_f`, `#sec-apply_other`, `#sec-create_fun`, `#sec-functional`, `#sec-functions_objects`, `#sec-sanity_check`, `#sec-why_functions` — определены, но не используются внутри файла (могут использоваться из других глав)
- Остальные проверки (TODO, Вы/Вам, слова-паразиты, p-value, Unicode-греческие, ifelse, fig.cap, hunspell) — чисто.

## 🔴 Критичные

1. **[строка 120]** `...внутри нее. вы ее можете мысленно представить` → Предложение начинается со строчной буквы после точки. Перестроить: «...внутри неё. Теперь вы можете мысленно представить её как операцию...» **(3/3)**

2. **[строка 101]** `` ```{r, error=TRUE} `` → Старый синтаксис чанка (§17). Заменить на ```` ```{r} ```` + `#| error: true` **(3/3)**

3. **[строка 244]** `callout-warning` с заголовком `## *Осторожно:*` → По §19, «Осторожно» = `callout-important`. Поменять тип на `callout-important`. **(3/3)**

4. **[строка 45]** ` -- ` → ` --- ` (em dash) **(3/3)**

5. **[строка 110]** `ошибки - это вовсе не обязательно` и `прописанная ошибка - чья-то забота` → дефис вместо em dash ` --- ` **(2/3)**

6. **[строка 136]** `самое важное из этого всего - это то, что` → дефис вместо em dash **(2/3)**

7. **[строка 200]** `Анонимные функции - это функции` → дефис вместо em dash **(2/3)**

## 🟡 Важные

1. **[строки 7, 47, 73, 198]** Вложенный курсив внутри жирного для терминов. Пример: `**анонимные функции *(anonymous function)****.*` → исправить на `**анонимные функции** *(anonymous function)*.` **(3/3)**

2. **[строка 138]** `**фабрикой функций**` — термин без английского перевода. Добавить `*(function factory)*`. **(2/3)**

3. **[строка 205]** `**лямбда-функций**` — повторное жирное / без перевода. Добавить `*(lambda function)*` при первом вводе. **(2/3)**

4. **[строка 291]** `[вот этот туториал](...)` → текст ссылки неинформативен (§24). Заменить на `[туториал по семейству apply() на DataCamp](...)`. **(2/3)**

5. **[строки 32, 34]** Нет пробела после `#` в комментариях: `#Функция ничего...` → `# Функция ничего...` **(2/3)**

6. **[строка 150]** Нет пробелов после запятых: `` `lapply()`,`sapply()` `` → `` `lapply()`, `sapply()` `` **(1/3, подтверждено)**

7. **[строка 112]** Пропущены запятые: «вспомнить в каких случаях» → «вспомнить, в каких случаях»; «оказывается достаточно чтобы» → «достаточно, чтобы»; «причину ошибки даже если» → «ошибки, даже если» **(1/3, подтверждено)**

## 🔵 Желательные

1. **Структура:** глава имеет итоговый абзац, но он относится только к `apply()` и переходу к `{purrr}`. Полноценного итога всей главы (функции + apply) нет. **(1/3)**

2. **[строка 7]** Очень длинное первое предложение (~80 слов). Рекомендуется разбить. **(2/3)**

3. **[строка 256]** «такой код будет не только более читаемым, но и в большинстве случаев более быстрым» — утверждение о производительности без краткого объяснения (векторизация → C). **(1/3)**

4. **[строка 159]** Рисунок вставлен через Markdown `![]()`, а не через Quarto-чанк с `#| fig-cap:`. Некритично, но при необходимости cross-reference затруднён. **(1/3)**

## Резюме

Глава написана живым разговорным языком, хорошо структурирована и логически последовательна. Главные проблемы — технические: массовое использование дефиса вместо em dash (7 случаев), один чанк в старом синтаксисе, неверный тип callout-блока, и систематическая ошибка в формате введения терминов (вложенный курсив внутри жирного). Все критичные находки — механические и легко исправимы. Содержательных ошибок в R-коде и статистике не обнаружено.
