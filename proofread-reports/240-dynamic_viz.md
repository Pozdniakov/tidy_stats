# Отчёт вычитки: 240-dynamic_viz.qmd
Дата: 2026-06-01

## Механические проверки (bash)

- **TODO-комментарии:** нет
- **Двойное тире / дефис в диапазонах / Вы с заглавной / слова-паразиты / p-value / Unicode-греческие:** нет
- **Устаревший синтаксис чанков:** строки 7, 35, 39, 45, 57 — все чанки используют старый формат `` ```{r, ...} ``
- **`library()` без `message: false`:** строка 40 (`library(plotly)`) — чанк на строке 39 не содержит `#| message: false`
- **`library("tidyverse")` (строка 10):** подавление есть (строки 8–9), ОК
- **Мёртвые якоря:** `#sec-html_other`, `#sec-html_w`, `#sec-plotly`, `#sec-vis_dynamic` — определены, но не используются как cross-references внутри файла (могут использоваться из других глав)
- **Hunspell:** не установлен в sandbox

## 🔴 Критичные

1. **[строка 53]** `«наведение на курсора на точки»` → лишний предлог «на». Исправить: «наведение курсора на точки». **(3/3)**

2. **[строка 5]** `«предоставляет интерфейс для работы с JavaScript визуализациями из R и вставлять их»` → нарушение синтаксического параллелизма. Исправить: «…для работы с JavaScript-визуализациями из R и вставки их в…» или «…и позволяет вставлять их в…». **(3/3)**

3. **[строки 7, 35, 45, 57]** Старый синтаксис чанков (```` ```{r, include=FALSE} ````, ```` ```{r, eval = FALSE} ````, ```` ```{r, echo = FALSE, results = 'asis'} ````). Мигрировать на `#|`-опции по §17. **(3/3)**

4. **[строка 39–40]** `library(plotly)` без `#| message: false`. По §17 чанки с `library()` требуют подавления сообщений. **(3/3)**

5. **[строка 79]** `«пакет `leaflet` - это»` → дефис вместо em dash. Должно быть ` --- `. **(2/3, Sonnet + Opus)**

6. **[строка 5]** `«*RMarkdown*»` → правильное написание: *R Markdown* (два слова, официальное название). **(1/3, Opus — перепроверено: действительно верно)**

## 🟡 Важные

1. **[строки 5, 33, 43, 55, 74, 78–80]** Пакеты без фигурных скобок: `htmlwidgets`, `plotly`, `ggplot2`, `echarts4r`, `leaflet`, `networkD3`. По §9 должно быть `{htmlwidgets}`, `{plotly}` и т.д., при первом упоминании со ссылкой. **(3/3)**

2. **[строка 74]** Голый URL `<http://gallery.htmlwidgets.org/>`. По §24 нужен описательный текст: «в [галерее htmlwidgets](http://gallery.htmlwidgets.org/)». **(3/3)**

3. **[вся глава]** Отсутствует итоговый абзац — глава обрывается на маркированном списке пакетов. По §6 нужен вывод «что узнали, что дальше». **(3/3)**

4. **[строка 1]** Нет вводного абзаца под заголовком `#` первого уровня — сразу идёт `##`. По §6 нужен краткий ввод (о чём глава, зачем читателю). **(2/3, Opus + Haiku)**

5. **[строка 5]** Слово «пакет»/«пакетов»/«пакеты» повторяется 4 раза в двух предложениях. Переформулировать. **(3/3)**

6. **[строка 5]** «в первую очередь» повторяется дважды в одном абзаце (строка 5). Убрать второе вхождение. **(2/3, Opus + Haiku)**

7. **[строка 33]** `«Один из самых распространенных средств»` → несогласованность: «один» (м.р.) + «средств» (ср.р.). Исправить: «одно из самых распространённых средств» или «один из самых распространённых инструментов». **(1/3, Sonnet — перепроверено: грамматически верно, «средств» здесь действительно требует «одно»)**

8. **[строка 53]** `«Не всегда это получается так, как хотелось бы»` — не объясняется, в чём именно ограничения `ggplotly()`. Добавить хотя бы один пример. **(1/3, Opus — перепроверено: полезная рекомендация для читателя)**

## 🔵 Желательные

1. **[строка 5]** Предложение `«htmlwidgets --- это пакет, в первую очередь, для разработчиков…»` слишком длинное (~40 слов). Разбить на два. **(2/3)**

2. **[строка 53]** `«ePUB»` — проверить единообразие написания по всей книге (EPUB vs ePUB). **(1/3, Sonnet)**

3. **[строка 22]** `guides(size=FALSE, colour = FALSE, fill = FALSE)` → в новых версиях `{ggplot2}` `FALSE` в `guides()` устарел, рекомендуется `"none"`. Не критично, но стоит обновить. **(1/3, Opus — перепроверено: верно для ggplot2 >=3.3.4)**

4. **[строки 45–51, 57–69]** Чанки с условием `knitr:::is_latex_output()` не имеют меток (`#| label:`). Рекомендуется для воспроизводимости. **(1/3)**

## Резюме

Глава очень короткая (81 строка) и содержит минимум прозы — в основном код. Основные проблемы: (1) все чанки используют устаревший синтаксис и требуют миграции на `#|`-формат; (2) пакеты систематически упоминаются без фигурных скобок; (3) грамматическая ошибка «наведение на курсора» и нарушение параллелизма в первом абзаце. Структурно глава не имеет ни вводного, ни итогового абзаца, что делает её незавершённой. Содержательно глава корректна — код работает, описания `{plotly}` и других виджетов точны.
